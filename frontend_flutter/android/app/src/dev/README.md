# Firebase Staging Config
Cole o arquivo `google-services.json` do projeto de STAGING nesta pasta.
O build system do Android irá selecionar este arquivo automaticamente ao rodar com `--flavor staging`.
