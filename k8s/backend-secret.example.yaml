apiVersion: v1
kind: Secret
metadata:
  name: cadife-backend-secret
  labels:
    app: cadife-backend
type: Opaque
stringData:
  # =========================================================================
  # IMPORTANTE: Este é apenas um TEMPLATE de exemplo para documentação.
  # NUNCA comite senhas reais no Git. 
  # Em produção, use SealedSecrets, ExternalSecrets (AWS/GCP) ou injeção via CI/CD.
  # =========================================================================
  DATABASE_URL: "postgresql+asyncpg://user:password@host:5432/cadife_db"
  REDIS_URL: "redis://host:6379/0"
  JWT_SECRET_KEY: "substituir_por_chave_aleatoria_segura_aqui"
  ENCRYPTION_KEY: "substituir_por_chave_fernet_aqui"
  HASH_KEY: "substituir_por_chave_hmac_aqui"
  OPENROUTER_API_KEY: "sk-or-v1-..."
  WHATSAPP_TOKEN: "EAA..."
  PHONE_NUMBER_ID: "123456789"
  VERIFY_TOKEN: "meu_token_secreto_webhook"
